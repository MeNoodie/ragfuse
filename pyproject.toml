[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ragfuse"
version = "0.1.0"
description = "A simple RAG package for loading and chunking documents"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
include = ["ragfuse*"]
